extends layout

block content
  .main.container.text-xs-center
    h1.display-3 Thank you #{name}!
    p.lead I won't email you unless its to say thanks again.
    a.btn.btn-info-outline.btn-lg(href='/') Go Home